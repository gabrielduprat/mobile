import MaterialCommunityIcons from '@expo/vector-icons/MaterialCommunityIcons';

export const Icon = ({name, size, color}: any) => {
    return <MaterialCommunityIcons name={name} size={size} color={color} />;
}